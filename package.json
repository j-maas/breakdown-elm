{
  "private": true,
  "dependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-format": "^0.8.1",
    "elm-live": "^3.4.0",
    "elm-test": "^0.19.0-rev5"
  },
  "scripts": {
    "start": "elm-live src/Main.elm --path-to-elm=node_modules/.bin/elm --dir=dist/ --before-build=./build.sh --start-page=./index.html",
    "start:no-sw": "elm-live src/Main.elm --path-to-elm=node_modules/.bin/elm --dir=dist/ --before-build=./build-no-sw.sh --start-page=./index.html",
    "test": "elm-test",
    "format": "elm-format ."
  }
}
